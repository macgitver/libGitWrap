
GET_FILENAME_COMPONENT(
    GITWRAP_CMAKE_DIR
    "${CMAKE_CURRENT_LIST_FILE}"
    PATH
)
SET(
    GITWRAP_INCLUDE_DIRS
        "@CONFIG_INC_DIRS@"
)

if(NOT TARGET GitWrap)
  INCLUDE("${GITWRAP_CMAKE_DIR}/GitWrapTargets.cmake")
ENDIF()

SET(GITWRAP_LIBRARIES           GitWrap)
